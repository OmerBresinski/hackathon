import styled from "styled-components";

export const BaseTextInput = styled.div`
    display: flex;
    flex-direction: column;
`;

export const Label = styled.div``;

export const Input = styled.input``;
